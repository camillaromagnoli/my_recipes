import 'package:flutter/material.dart';

class MyRecipesListItem extends StatelessWidget {
  const MyRecipesListItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
